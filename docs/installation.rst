============
Installation
============

At the command line::

    $ easy_install django-pong

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pong
    $ pip install django-pong
